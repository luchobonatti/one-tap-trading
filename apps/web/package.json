{
  "name": "@one-tap/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "echo 'stub: replace when Next.js is set up'",
    "dev": "echo 'stub: replace when Next.js is set up'",
    "lint": "echo 'stub: replace when oxlint is set up'",
    "typecheck": "echo 'stub: replace when tsconfig is set up'",
    "test": "echo 'stub: replace when vitest is set up'",
    "format": "echo 'stub: replace when oxfmt is set up'",
    "clean": "echo 'stub: replace with rm of .next, dist'"
  }
}
