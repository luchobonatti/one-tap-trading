{
  "name": "@one-tap/indexer",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "dev": "cargo run",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "format": "cargo fmt",
    "clean": "cargo clean"
  }
}
