# Repo-level pnpm configuration
# Note: We intentionally do NOT set `ignore-scripts=true` here.
# If you need to disable lifecycle scripts in a constrained environment,
# run `pnpm install --ignore-scripts` or use an environment-specific config instead.
