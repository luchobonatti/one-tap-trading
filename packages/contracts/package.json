{
  "name": "@one-tap/contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "forge build",
    "lint": "forge fmt --check",
    "test": "forge test -vvv",
    "format": "forge fmt",
    "clean": "forge clean"
  }
}
