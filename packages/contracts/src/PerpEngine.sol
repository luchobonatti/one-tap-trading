// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

import { IPerpEngine } from "./IPerpEngine.sol";

/// @title PerpEngine
/// @notice Stub implementation — will be replaced with real logic in a later issue.
///         Exists only so the compiler validates that IPerpEngine is coherent.
abstract contract PerpEngine is IPerpEngine { }
